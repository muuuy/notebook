const UploadPicture = () => {
  // Handles when user selects an image
  const handleSelect = () => {};

  // Handles when user wants to submit and image
  const handleUpload = () => {};

  return (
    <div>
      <p>UPLOAD PICTURE</p>
      <button onClick={handleSelect}>Select File...</button>
      <button onClick={handleUpload}>Upload</button>
    </div>
  );
};

export { UploadPicture };
